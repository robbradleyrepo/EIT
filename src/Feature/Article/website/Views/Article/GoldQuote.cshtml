@using Glass.Mapper.Sc.Web.Mvc
@model LionTrust.Feature.Article.Models.IArticleText
@if (Model == null && !Sitecore.Context.PageMode.IsExperienceEditor)
{
    return;
}
<div class="article-with-big">
    <span class="gold-qoute font-500">
        @Html.Glass().Editable(m => m.Copy)
    </span>
</div>