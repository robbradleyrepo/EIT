@model LionTrust.Feature.Fund.Models.IFundCentre
@if (Model != null)
{    
    <iframe class="mt-3 mb-5" id="childFrame"
            src="@Model.FundCentreIFrameRootUrl"
            height="200" width="100%" frameborder="0" scrolling="no"
            webkitallowfullscreen mozallowfullscreen allowfullscreen>
    </iframe>

}