@using Glass.Mapper.Sc.Web.Mvc
@model LionTrust.Feature.Fund.FundClass.FundClassViewModel
@if (Model == null && !Sitecore.Context.PageMode.IsExperienceEditor)
{
    return;
}
<div class="container container-p-0">
    <div class="row">
        <div class="col-lg-4 offset-lg-8">
            <div class="class-dropdown">
                <div class="class-dropdown__title">
                    @Html.Glass().Editable(m => m.FundSelector.DropDownLabel)
                </div>
                <div class="class-dropdown__select">
                    <select name="classes" id="classes" class="fund-chart-dropdown">
                        @if (Model.FundSelector.Fund != null)
                        {
                            foreach (var item in Model.FundSelector.Fund.Classes)
                            {
                                if (Model.DefaultCitiCode == item.CitiCode)
                                {
                                    <option value="@item.CitiCode" selected>@item.ClassName</option>
                                }
                                else
                                {
                                    <option value="@item.CitiCode">@item.ClassName</option>
                                }

                            }
                        }
                        else if (Sitecore.Context.PageMode.IsExperienceEditor)
                        {
                            @:Please select a fund for the component
                        }
                    </select>
                    <i class="article-filters__icon-arrow article-filters__icon-arrow_select"></i>
                </div>
            </div>
        </div>
    </div>
</div>